## Issues

(Initial - no issues yet)

- `npx expo run:ios` fails in Swift import resolution: `AppDelegate.swift:1:8 no such module 'Expo'`.
- Xcode reports the generated `ios/Pods/Pods.xcodeproj` as damaged with `-[XCSwiftPackageProductDependency _setSavedArchiveVersion:]`.
- Cleaning DerivedData and rerunning `pod install` did not resolve the iOS build blocker.
